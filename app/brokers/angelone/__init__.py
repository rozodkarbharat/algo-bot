"""
Angel One SmartAPI broker adapter.

Submodules:
  auth.py            — Authentication, session management, token refresh
  historical_data.py — Historical OHLCV candle fetching

The concrete order-execution adapter (implementing BaseBroker) will be
added in a future step when live trading is implemented.
"""
